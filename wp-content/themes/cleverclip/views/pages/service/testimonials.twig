<div class="service__testimonials">
  <div class="service__testimonials__box">
    <div class="service__testimonials__wrapper">
      {% for testimonial in post.get_field('testimonials_list') %}
        <article class="service__testimonial">
          <div class="service__testimonial__wrapper">
            <figure class="service__testimonial__media">
              <img class="service__testimonial__media__image" data-src="{{ testimonial.image }}">
            </figure>

            <div class="service__testimonial__content">
              <p class="service__testimonial__description">
                {{ testimonial.description }}
              </p>

              <div class="service__testimonial__author">
                <p class="service__testimonial__author__column">
                  <strong class="service__testimonial__author__title">
                    {{ testimonial.author }}
                  </strong>

                  <span class="service__testimonial__author__description">
                    {{ testimonial.role }}
                  </span>
                </p>

                <div class="service__testimonial__author__column">
                  <img class="service__testimonial__author__image" data-src="{{ testimonial.company }}">
                </div>
              </div>
            </div>
          </div>
        </article>
      {% endfor %}
    </div>

    <div class="service__testimonials__navigation">
      <button class="service__testimonials__navigation__button service__testimonials__navigation__button--previous">
        Previous
      </button>

      <button class="service__testimonials__navigation__button service__testimonials__navigation__button--next">
        Next
      </button>
    </div>

    <div class="service__testimonials__pagination">
      {% for testimonial in post.get_field('testimonials_list') %}
        <button class="service__testimonials__pagination__button" data-index="{{ loop.index0 }}">
          {{ loop.index0 }}
        </button>
      {% endfor %}
    </div>
  </div>
</div>
